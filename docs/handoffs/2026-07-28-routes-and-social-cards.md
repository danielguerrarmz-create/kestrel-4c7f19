# 2026-07-28 — Real paths, per-page metadata, and a sharing card that exists

Branch `seo/routes-and-cards`, forked from `origin/main` @ `23bf223`. Not pushed; Daniel
authorizes all git state changes.

---

## What

Four things, in dependency order. Each one was blocked by the one before it.

**1. The four public pages moved from `#/` hash routes to real paths.**
`#/gallery` → `/gallery`, `#/questions` → `/questions`, `#/about` → `/about`, home stays `/`.
`src/routing.ts` is a History API router now: `useRoute` reads `location.pathname` through
`useSyncExternalStore`, subscribing to `popstate` plus its own `navigate`. Links stay real
anchors (`<a href="/gallery">`) and a single delegated click handler upgrades a plain left-click
into a `pushState`; there is no `<Link>` component, so a plain `<a>` cannot be a bug.
`vercel.json` rewrites every app path to `index.html` while `/assets/*`, `/agent/*`,
`/robots.txt`, `/llms.txt` and `/sitemap.xml` continue to serve from disk. A **legacy-hash shim**
(`legacyHashRedirect`, run by `installRouter()` before the first render) rewrites an inbound
`/#/gallery` to `/gallery` in place, so every link shared over the past week still works.

**2. The sharing card.** `public/assets/social/og-card.jpg` — a real 1200x630 JPEG, 251 KB,
generated by `npm run gen:og` from `public/assets/gallery/01-wisteria-walk.webp`. index.html
carries the whole tag set (`og:image` absolute, `:width`, `:height`, `:alt`, `:type`,
`og:site_name`, `og:locale` `en_GB`, `twitter:card` `summary_large_image` + `twitter:image`), and
the copy was rewritten to describe the practice as it is.

**3. Per-page metadata.** `src/seo.ts` owns title, description, canonical and OG title/description
for each route, applied on navigation from `Root` via `useDocumentMeta`.

**4. sitemap.xml + structured data.** `public/sitemap.xml` (generated, drift-guarded), referenced
from `robots.txt`. `Organization` static in index.html; `FAQPage` injected on `/questions` only,
built from `QUESTIONS` itself.

**5. Vercel Web Analytics** (`@vercel/analytics`), mounted from `main.tsx` as `<SiteAnalytics />`,
production-only and route-aware. Daniel chose it over Plausible and Search Console: the site is
already on Vercel, the free tier covers this traffic, and it sets no cookie, so there is no consent
banner to design.

Also: `useFragmentScroll` in Root, so a cold `/questions#cost` lands on the cost answer. See
*Why* below — this is new surface, not a restoration.

---

## Why

**The hash was costing the site its entire search surface.** A fragment is never sent to a server
and is not indexed as a distinct page, so `bowerbuild.org/#/gallery` was, to every crawler and
every link unfurler, the single URL `bowerbuild.org/`. Four pages of real writing (seven
renderings, seven answered questions, the practice's whole history) competed as one document with
one `<title>` — the word "Bower" — one description, no canonical, and no sitemap
(`/sitemap.xml` was a 404). Nothing could be linked to, ranked, or canonicalized separately.
Everything else in this handoff was downstream of that: per-page titles are meaningless without
per-page URLs.

**The card was advertising a product that no longer exists.** index.html had no `og:image` at all,
so a pasted link showed a blank card with a generic browser icon — under an `og:title` reading
"Bower: shape a living pavilion, priced fixed as you shape it" and a description selling "a
generative design engine constrained by a fabrication grammar". That is the studio/draw tool,
which came off the live site on 2026-07-21. The site's whole argument is visual and the card was
showing none of it while describing something a visitor cannot open.

**Three judgement calls worth recording:**

- **JPEG, not WebP, and a separate asset even though the site already ships this picture.**
  Unfurlers are unreliable with WebP and their failure mode is *no image*, which is the exact bug
  being fixed. The source is 1920x1046 (1.836:1) and the slot is 1.905:1; handing an unfurler the
  wrong ratio lets *it* choose the crop, which is how a canopy gets decapitated.
- **index.html carries NO static `<link rel="canonical">` and no static `og:url`.** In a
  single-HTML SPA a static canonical tells a non-rendering crawler that `/gallery` is a duplicate
  of `/` — the opposite of the truth, and worse than having none. Both are injected per route.
- **`FAQPage` is injected per route, `Organization` is static.** Organization is true of every URL
  so it can be JS-free; claiming every URL is an FAQ page is a Google policy violation as well as
  a lie.

**On the prices: THE HEAD SELLS, THE PAGE PRICES. There are no figures in the meta layer at all.**

The first version of this work quoted the commission figure in two meta descriptions and two
og:descriptions as string literals, with a test binding them to the questions page's own answer.
That test passed, and the design was still wrong. `pages/questions/copy.ts` owns the published
price, it is bound to `ui/priceCopy.ts`'s `COMMISSION_FROM`, and `COMMISSION_BREAKEVEN_GBP` guards
it as a cost fact the published figure must clear. Adding a fifth and sixth copy in the head is one
fact in several places, which is this repo's most expensive recurring bug, and the head is the
worst place to keep a copy: a link card is cached by iMessage, WhatsApp, Slack and LinkedIn for
months, outside our control, and gets screenshotted. **A green test cannot un-cache a frozen
card.** An absence is a stronger invariant than a derivation here, because there is nothing left to
keep in sync. The SEO cost is near zero: Google generates query-relevant snippets from page
content, so a "what does a garden pavilion cost" search still gets the price from the page that
owns it.

`seo.test.ts` now pins the absence: no currency symbol and no bare thousands figure may appear in
any title, description or og:description, and none in `index.html` either.

**The one exception is the FAQ schema, and it is not a copy.** `faqPageJsonLd()` maps over
`QUESTIONS`, so the cost answer is the page's own sentence serialized, not retyped. Omitting it
would be the real error, since cost is the question an answer engine is most often asked about a
Bower. So the schema runs the **same break-even guard** `questions/copy.test.ts` runs on the page,
on the schema's own output, scoped the same way (the commission sentence, not every pound sign:
Stage 1 and Stage 2 professional fees are supposed to sit below break-even on the object). It also
pins that none of the demo constants (`COMMISSION_DEMO_FIGURE`, `COMMISSION_ANCHOR_GBP`,
`COMMISSION_FLOOR_GBP`, all still anchored to the retracted figure, dev-only, knowingly
flagged-not-fixed) can reach a shipping surface. Those constants were not touched.

**On analytics, and why it is manual rather than auto-track.** The SDK has two modes. With no
`route` prop, the remote script patches `history.pushState` and would in fact see our hand-rolled
router (verified by reading `node_modules/@vercel/analytics/dist/react/index.mjs` rather than
assuming). Passing both `route` and `path` sets `disableAutoTrack` and hands over each pageview
explicitly. We pass both, because this site has a catch-all: **every unrecognised URL renders the
home splash.** Under auto-track, `/typo`, `/wp-login.php` and every scanner probe would each open
its own row in the dashboard while actually being the home page, fragmenting the one number anybody
wants. So `route` is the canonical route production serves and `path` is the real URL, and the
mapping is `metaForPath` — the same "what does production actually serve here" decision that
already picks the canonical tag and the title, rather than a second list that can drift.

**On `useFragmentScroll`.** Under the hash router `/questions#winter` could not exist — the hash
*was* the route, so it parsed as the unknown path `/winter` and threw you to the splash. Real
paths made per-answer deep links possible, and FAQ structured data keyed on the same `id`s makes
them likely (an answer engine or a Google "jump to" link pointing at `#cost`). Measured on the
built site before the fix: a cold `/#register` happened to land and a cold `/questions#winter` did
not, because a browser stops retrying a fragment before a client-rendered page mounts, and the
home is heavy enough that the retry outlasted React. That is the works-on-my-page inconsistency a
sleep would paper over, so it waits for the element rather than for a clock.

---

## Verify

`npm run build`, `npx tsc --noEmit`, `npx tsc --noEmit -p tsconfig.tests.json`: all green.
**vitest: 909 passed, 63 files, run three times** (the repo's own rule about quoting a suite
number after one run).

New tests: `src/routing.test.ts` grew path normalization, the legacy-hash shim, the click
delegate's claim rules, the `vercel.json` rewrite regex, and the `main.tsx`/`Root.tsx` wiring —
31 tests in that file now. `src/seo.test.ts` (new, 17) covers the card file, the tag set, the
price invariants, the dash rule, and the schema. `src/seo.generated.test.ts` (new, 4) generates
and drift-guards the sitemap.

Beyond the suite, the **built** site was driven in headless Chrome against `npx vite preview`
(scripts were scratch, not committed):

| Check | Result |
|---|---|
| `/`, `/gallery`, `/questions`, `/about` each render their own page | 4/4 |
| each carries its own `<title>`, description, canonical, `og:url` | 4/4, all `https://www.bowerbuild.org/...` |
| `FAQPage` present on `/questions`, absent on the other three | pass; 8 questions, cost answer carries `£350,000 including VAT`, no `£150,000` |
| legacy `#/gallery`, `#/questions`, `#/about`, `#/`, `/?species=x#/about` | all redirect to the real path, query preserved |
| `#register` is NOT rewritten to `/register` | stays a fragment, browser scrolls (scrollY 2682) |
| cold `/questions#cost`, `#winter`, `#planning`, `#ring` | 4/4 land with the heading at y≈200 |
| `/questions` with no fragment | opens at scrollY 0 |
| nav click | `pushState`, no document reload, head updated; back button restores both |
| `mailto:` click on `/questions` | not swallowed by the delegate, router stays on `/questions` |
| `/studio`, `/draw`, `/engine`, `/shape`, `/sculpt`, `/lab/gongbi`, `/about/tree` in the prod build | 7/7 fall through to the home splash |
| `/sitemap.xml`, `/robots.txt`, `/agent/questions.md`, `/llms.txt` | 200, correct content types |
| **no gated surface in the production bundle** (`node qa/bundle-leak.mjs`) | clean across 8 surfaces, 639 KB scanned; **and the probe was proven able to fail** by making `AboutTreePage` a static import again, which it caught (`data-tree-track`, `data-tree-canvas`, dist 639 KB -> 687 KB) |
| **`og:image` resolves to a real file** | 200 `image/jpeg`, 251 KB, and the **served bytes decode to 1200x630** (JPEG SOF marker parsed directly, not asked of sharp) |
| analytics under `npm run dev` | `window.va` undefined, no script tag, **zero network requests** |
| analytics in the production build | injects `/_vercel/insights/script.js` (production path, `vam: production`), one pageview queued on load |
| analytics across client-side navigation | nav clicks + back button reported `/` -> `/questions` -> `/gallery` -> `/questions` |
| analytics on a junk URL | `/wp-login.php` reports `route: "/"` with `path: "/wp-login.php"`, so no row fragmentation |
| **bundle cost** | **+3.9 KB total JS**: +417 bytes on the critical path, plus a 3.5 KB chunk fetched lazily after mount. `dist/index.html` does not preload it |

### The card, before and after

| | Before | After |
|---|---|---|
| Image | **none** (blank card, generic browser icon) | 1200x630 JPEG, the wisteria walk |
| Title | "Bower: shape a living pavilion, priced fixed as you shape it" | "Bower: living garden pavilions" |
| Description | "A generative design engine constrained by a fabrication grammar: every form you can touch is buildable, priceable, and designed for the plant that grows through it." | "A design practice building woven timber pavilions for gardens, planted with the climbers that grow into them. Each one drawn for its own garden." |
| Twitter card | `summary` (small square thumbnail) | `summary_large_image` |
| `og:site_name` / `og:locale` | absent | `Bower` / `en_GB` |

The crop: 38px off the **bottom** (3.6%, plain decking), then an exact 0.625 scale in both axes.
The arches, the canopy and both sets of cafe tables are intact. `scripts/gen-og-card.mjs` throws
rather than guesses if the source ratio ever changes enough to need a width crop instead.

---

## Left

**1. NOTHING IS PRERENDERED, so link unfurlers still get the home's card copy on all four URLs.**
This is the honest limit of the change and the biggest remaining item. Unfurlers (iMessage,
WhatsApp, Slack, LinkedIn) do not execute JavaScript, so they read index.html's static head no
matter which path was pasted. They now get a correct, current card with a real image — the bug
Daniel raised is fixed — but a shared `/questions` link shows "Bower: living garden pavilions"
rather than "what one costs". Google renders JS, so the per-route heads, canonicals and FAQ schema
do reach it.

The fix is a post-build step emitting `dist/gallery.html`, `dist/questions.html` and
`dist/about.html` with `metaForPath` baked into the head, plus `vercel.json` rewrites pointing
each path at its own file. It is roughly 40 lines and reuses `src/seo.ts` as-is. **I did not do it
here**: it changes the deploy's routing shape, and a mistake there 404s the live site. It wants a
Vercel preview deployment to verify, which is Daniel's call to make.

**2. ANALYTICS NEEDS ONE CLICK IN THE VERCEL DASHBOARD, or it records nothing.** Web Analytics has
to be enabled for the project (Project -> Analytics -> Enable). Until then `/_vercel/insights/script.js`
404s, the SDK logs a console line saying exactly that, and the site is otherwise unaffected. Nothing
in the repo can do this step.

A related trap was found and fixed before it shipped: **the SPA catch-all rewrite in `vercel.json`
matched `/_vercel/insights/script.js` and `/_vercel/insights/view`**, so the script would have
loaded the HTML shell and every pageview would have posted into a 200 that meant nothing. Zero
errors, zero visible symptoms, a dashboard reading zero forever. `_vercel/` is excluded now and
`routing.test.ts` pins it.

**3. `qa/header-nav.mjs` was missing `/questions` in `PUBLIC_HREFS`** (added the same day the page
shipped, never added to the probe). Fixed while updating the probes to real paths, but it means
that probe has not passed since 2026-07-28 and nobody noticed. Worth a run.

**4. The QA probes were mechanically rewritten to real paths and NOT re-run** (they need a dev
server and a screenshot review loop). The URL rewrite is trivial; the risk is low but non-zero.
`qa/base.mjs`'s `aboutUrl` note keeps the old hazard as a tombstone because the failure mode has
not changed: a probe pointed at the wrong page still returns numbers.

**5. Nothing is submitted to Google.** After deploy, `sitemap.xml` should be submitted in Search
Console and the four URLs requested for indexing; the old `/#/...` URLs were never indexed as
pages, so there is no redirect debt to clear, but there is also no existing ranking to inherit.

**6. Two tests that could no longer fail were found and fixed while doing this**, and there may be
more of the same class. `HeroReveal.test.ts` asserted `not.toContain('#/studio')`, which after the
path migration is a string the page cannot produce under any bug; it is an href sweep against the
live public routes now, and I verified it fails by pointing a hero CTA at `/studio`.
`qa/header-nav.mjs`'s `PUBLIC_HREFS` was missing `/questions`. **A routing change silently converts
absence-based guards into no-ops** wherever they pinned the old URL shape, so it is worth grepping
for other `not.toContain('#/` survivors if more routing work lands.

**7. `og:locale en_GB` and `<html lang="en-GB">`** — I changed `lang` from `en`. The site sells to
UK gardens and the copy is British. Flagging it because it was not in the brief.

**8. The contact in the Organization schema is Clay's personal mobile (a US number) and gmail**,
because that is what `config.ts` publishes. Structured data makes it machine-readable in a way the
rendered page did not. Already flagged in `config.ts` as the first thing to revisit; this raises
the stakes slightly.

---

## Files

**New**
- `src/seo.ts` — per-page head, `Organization`/`FAQPage` builders, sitemap generator, `SITE_ORIGIN`, `OG_CARD`
- `src/seo.test.ts`, `src/seo.generated.test.ts`
- `scripts/gen-og-card.mjs` (+ `npm run gen:og`)
- `qa/bundle-leak.mjs` — scans `dist/` for gated surfaces; self-checks its own markers
- `src/analytics.tsx` — `SiteAnalytics`, production-gated, route-aware; `analyticsRouteFor`
- `src/analytics.test.ts`
- `vercel.json`
- `public/sitemap.xml` (generated), `public/assets/social/og-card.jpg` (generated)

**Rewritten**
- `src/routing.ts` — path router, legacy-hash shim, click delegate, `useFragmentScroll`, `PUBLIC_ROUTES`
- `index.html` — head, card tags, static `Organization`, noscript list

**Edited**
- `src/Root.tsx`, `src/main.tsx` (`installRouter()` before render)
- `src/agent/mirror.ts` + regenerated `public/agent/*.md`, `public/llms.txt`
- `public/robots.txt` (sitemap reference, `questions.md` was missing)
- `src/ui/Footer.tsx`, `src/pages/splash/SplashHeader.tsx`, `src/pages/QuestionsPage.tsx`,
  `src/pages/ShapePage.tsx`, `src/pages/SculptPage.tsx` (two literal `#/` hrefs), `src/state/store.ts`
- `src/routing.test.ts`, `src/pages/SplashPage.test.ts`, `src/pages/engine/EnginePage.test.ts`,
  `src/state/store.test.ts`
- `qa/*.mjs` (19 files, probe URLs), `CLAUDE.md`
